<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aks.MyLabs">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".User_area" />
        <activity android:name=".Test" />
        <activity android:name=".Profile" />
        <activity android:name=".SimpleList" />
        <activity android:name=".LabSelection" />
        <activity android:name=".LabRate" />
        <activity android:name=".LabDetail" />
        <activity android:name=".Address" />
        <activity
            android:name=".navdraw"
            android:label="@string/title_activity_navdraw"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Invoice"></activity>
    </application>

</manifest>